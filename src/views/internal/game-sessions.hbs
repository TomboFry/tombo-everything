{{> pageHeader title=title url=backUrl }}

{{> internal/game-session gameId=gameId gameOptions=gameOptions }}
<hr/>

{{#each sessions as |session| }}
	{{> internal/game-session gameId=../gameId data=session gameOptions=../gameOptions }}
{{/each}}

{{> pagination route="/games/session" pagination=pagination }}
