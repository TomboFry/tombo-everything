<div class="sectioncontainer">
	<div class="row">
		<span class="left">📺</span>
		<span class="right">
			<strong>
				<a
					href="{{data.url}}"
					target="_blank"
					rel="noreferrer noopener"
				>
					{{data.title}}
				</a>
			</strong>
		</span>
	</div>
	<div class="row">
		<span class="left">👤</span>
		<span class="right">{{data.channel}}</span>
	</div>
	<div class="row">
		<span class="left"></span>
		<time
			title="{{data.created_at}}"
			datetime="{{data.created_at}}"
			class="right"
		>
			<small>
			{{#if showlink }}
				<a href="/youtube/{{data.id}}">{{data.timeago}}</a>
			{{else}}
				{{data.timeago}}
			{{/if}}
			</small>
		</time>
	</div>
</div>
